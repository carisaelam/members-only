<div id="<%= dom_id post %>">
  <h3><%= link_to post.title, post_path(post) %></h3>

  <p><%= post.body %></p>

<% if member_signed_in? %>
  <p>Posted by: <%= post.member.email %></p> 
<% end %>
</div>
